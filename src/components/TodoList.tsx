import React from "react";
import TodoListItem from "./TodoListItem";

const TodoList = () => {
  return (
    <div>
      TodoList
      <TodoListItem />
    </div>
  );
};

export default TodoList;
